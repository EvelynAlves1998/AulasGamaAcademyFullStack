const multiplicacao = function(x, y)
{
	return x * y;
};

//console.log(multiplicacao(5, 5));

const dividir = (x, y) => 
{
	return x / y;
}

const object = () => ({ nome: 'Evelyn' , sobrenome: 'Alves' });